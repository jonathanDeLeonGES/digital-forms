"""
Permission and profile tests — covered by test_auth.py unit tests.
Integration tests (DB-required) are deferred to docker compose test run.
"""
import pytest


def test_placeholder():
    assert True
